/* GLOBAL STYLE */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: Arial;
    background: #f0f4f7;
}

/* NAVIGATION */
nav {
    background: #002b5b;
    padding: 15px;
    text-align: center;
}

nav a {
    color: white;
    margin: 15px;
    text-decoration: none;
    font-size: 18px;
}

nav a:hover {
    color: #ffdd00;
}

/* HEADER BANNER */
header {
    background: url('event-banner.jpg') no-repeat center/cover;
    height: 300px;
    border-bottom: 5px solid #002b5b;
}

/* HOME PAGE CONTENT */
section.home-content {
    padding: 20px;
}

.highlight-video {
    margin-top: 20px;
    display: flex;
    justify-content: center;
}

/* BOX MODEL DEMO */
.box {
    padding: 15px;
    margin: 15px 0;
    border: 2px solid #004b8d;
}

/* FLEXBOX */
.flex-row {
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
}

/* GALLERY WITH CSS GRID */
.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 15px;
    margin: 20px;
}

.gallery-grid img {
    width: 100%;
    height: 180px;
    border-radius: 10px;
    object-fit: cover;
}

/* CONTACT PAGE */
.contact-box {
    padding: 20px;
    border-radius: 10px;
    background: white;
    width: 60%;
    margin: auto;
}

/* ATTRIBUTE SELECTOR */
input[type="text"], input[type="email"] {
    border: 2px solid #004b8d;
    padding: 10px;
    width: 100%;
    margin-top: 10px;
}

/* PSEUDO-ELEMENT */
h1::after {
    content: "✨";
    margin-left: 10px;
}

/* RESPONSIVE DESIGN */
@media (max-width: 768px) {
    nav a {
        display: block;
        margin: 10px 0;
    }

    .contact-box {
        width: 90%;
    }
}
